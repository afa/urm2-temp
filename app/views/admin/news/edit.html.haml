= form_for :news_article, :url => admin_news_index_path, :html => {:method => :put} do |f|
 %div
  =f.label :name
  = f.text_field :name
 %div
  = f.label :body
  = f.text_area :body
 %div
  = f.submit "save"
