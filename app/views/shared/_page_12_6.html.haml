.pagination{:style => "#{defined?(opts) && opts.has_key?(:align) ? "text-align:#{opts[:align]};" : ""}"}
 - (1..9).each do |curr|
  - if curr == page
   %span.current= curr
  - else
   %a{:href => url_for(parms.merge(:page => curr))}= curr
 %span ...
 %a{:href => url_for(parms.merge(:page => pages))}= pages
