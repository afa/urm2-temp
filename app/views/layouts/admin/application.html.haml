!!!
%html
 %head
  %title Urm
  = stylesheet_link_tag "application"
  = javascript_include_tag "application"
  = csrf_meta_tag
 %body
  #header
   - flash.each do |key, value|
    .flash{:class => key}=h value
   - if logged_in?
    = button_to t(:sign_out), admin_sessions_path, :method => :delete
   - else
    = link_to t(:sign_in), new_admin_session_path
  #content
   = yield

