-# Фильтры

-# Дата отгрузки: даты c/по (см. календарик в шаблоне 008, compel_html_008/orders.html), params.date_from, params.date_to
-# Наименование: строка, params.item_name
-# Только мои позиции: вкл/выкл, params.only_my
-# Только ДМС: вкл/выкл, params.only_dms
-# Таблица:

.nav-title
 %span= link_to "Заказы", orders_path
 %span >
 %span.current Заказ #{params[:id]}

.content-wrap
 .table-navigation-block{:class => "scroll"}
  .table-block
   .table-scrolling
    .handle_sale
     .make_order
      .header выставление счета
      = form_for :order, :url => order_path, :html => {:method => "GET"} do |f1|
       .param= select_tag("order[#{params[:id]}][order_action]", "<option value=\"make\">Создать</option><option value=\"send\">Отправить</option><option value=\"make_send\">Создать и отправить</option>".html_safe)
       .link [#{t(:go)}]
     .close_sale
      .header= t(:close_sale) #закрыть заказ
      = form_for :order, :url => order_path, :html => {:method => "GET"} do |f2|
       .param= text_area_tag "order[#{params[:id]}][order_cancelation_reason]"
       .link [#{t(:close)}]

    = render :partial => "orders/info", :locals => {:order => @order_info}

    = render :partial => "shared/pagination", :locals => {:page => @lines.page.to_i, :pages => @lines.total.to_i, :opts => {:show_numbers => false, :show_prevnext => true, :show_firstlast => true, :show_current => true, :text_before => "Записей: #{@lines.records}&nbsp;&nbsp;Страниц: #{@lines.total}&nbsp;&nbsp;".html_safe, :text_before_current => "Страница: "}}
    -# = render :partial => "shared/pagination", :locals => {:page => @lines.page.to_i, :pages => @lines.total.to_i}
    %table.table-style{:class => "order-list"}
     %tr
      %th
      %th ROHS
      %th= t(:item_name)
      %th Производитель
      %th Количество
      %th Цена
      %th Сумма
      %th Поставка
      %th Резерв
      %th В обработке складом
      %th Получено
      %th Продано
      %th Дата готовности
      %th Ожидаемая дата поставки
      %th Комментарий
      %th Требования
      %th Контактное лицо
      %th Проект

     = render :partial => "orders/showed_line", :collection => @lines.items

    .handle_lines
     .reserve_lines
      = form_for :order, :url => order_path, :html => {:method => "GET"} do |f3|
       .link [#{t(:reserve)}]
       .link [#{t(:unreserve_all)}]
     .pick_lines
      = form_for :order, :url => order_path, :html => {:method => "GET"} do |f4|
       .param= render :partial => "main/pick_options"
       .link [#{t(:pick)}]
     .reserve_transfer
      = form_for :order, :url => order_path, :html => {:method => "GET"} do |f5|
       .param= select_tag "order[#{params[:id]}][transferable_order]", "<option></option>".html_safe #TODO: place valid options
       .link [#{t(:transfer_sales_lines)}]
     .remove_lines
      = form_for :order, :url => order_path, :html => {:method => "GET"} do |f6|
       .param= text_area_tag "order[#{params[:id]}][erase_reason]"
       .link [#{t(:remove)}]
     .price_request
      .param ???
-# :javascript
 if($(".calendar-input").length > 0){
 $(".calendar-input").datepicker({ dateFormat: 'yy-mm-dd', popupContainer: $('#ui-datepicker-div-container')});
 }


