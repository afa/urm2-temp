%div
 = current_user.username
 = render :partial => "accounts/info", :collection => current_user.accounts
%div
 parent:
 %div
  = @parent.username
%div
 children:
 - @children.each do |u|
  %div
   = u.username
