= form_for :user, :url => users_path do |f|
 .field
  = f.label :username
  = f.text_field :username
  %span.error #{@user.errors.on(:username)}
 .field
  = f.label :ext_hash
  = f.text_field :ext_hash
  %span.error #{@user.errors.on(:ext_hash)}
 = f.submit "save"
