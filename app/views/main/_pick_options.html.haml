- pref = defined?(opts) ? (opts.try(:[], :prefix) || "") : ""
%h3 Параметры бронирования
-# p ":::deliv", @deliveries
- unless @deliveries.empty?
 .padded
  %span
   -# %span{:style => "float: left;"}Способ поставки:
   = render :partial => "shared/select_list", :locals => {:list_items => @deliveries, :name => "#{pref}delivery_type", :title => t(:delivery_type), :opts => {:use_system => true} }
   .clear
 .padded
  %span Дата готовности:
  %input.calendar-input{:type => "text", :name => "#{pref}date_picker", :value => Date.today.strftime("%Y-%m-%d")}

