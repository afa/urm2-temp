.help-link
 = link_to "[?]", main_help_path(HelpArticle.where(:key => 'main.index').first.try(:id))
- unless @news.blank?
 %h2= t(:news)
 = render :partial => "news/item_on_main", :collection => @news
