if($("tr.analog_item_#{@after}").length > 0){
$("tr.analog_item_#{@after}").remove();
}
$("#{escape_javascript(@items.empty? ? render(:partial => "analog_empty", :locals => {:after => @after}) : render( :partial => "analog_line", :collection => @items, :locals => {:after => @after}))}").insertAfter($("tr.item_#{@after}").last());
- unless @items.empty?
 $("#{escape_javascript(render(:partial => "analog_header", :locals => {:after => @after}))}").insertAfter($("tr.item_#{@after}").last());
 if($("tr.gap_#{@after}").length == 0  ){
 $("#{escape_javascript(render(:partial => "gap_line", :locals => {:after => @after}))}").insertAfter($("tr.dms_item_#{@after}").add("tr.analog_item_#{@after}").add("tr.info_item_#{@after}").last());
 }
$("tr.analog_item_#{@after} th .plus").click(function(){
$("tr.analog_item_#{@after}").remove();
if($("tr.dms_item_#{@after}").add("tr.info_item_#{@after}").length == 0){
$("tr.gap_#{@after}").remove();
}
});
