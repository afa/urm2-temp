$("#{escape_javascript(@items.empty? ? render(:partial => "empty_line") : render( :partial => "analog_line", :collection => @items, :locals => {:need_links => false}))}").insertAfter("tr##{@after}");
- unless @items.empty?
 $("#{escape_javascript(render(:partial => "analog_header"))}").insertAfter("tr##{@after}");
